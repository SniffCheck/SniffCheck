Public-release drop-in set. Copy these over the matching files in the public repo:

  CREDITS.md                      -> repo root
  NOTICE                          -> repo root (new file)
  manifest.json                   -> repo root
  firmware/sniffcheck-merged.bin  -> firmware/   (full snapshot of the current build)
  firmware/checksums.txt          -> firmware/   (optional)

These are public-clean: no internal provenance, no tooling fingerprints.
